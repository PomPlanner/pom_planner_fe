<div class="search-results">
  <% if @videos.present? %>
    <% @videos.each do |video| %>
      <%= video.title %>
      <%= video.url %>
      <%= video.thumbnail %>
      <!-- other things we want -->
    <% end %>
  <% else %>
    Please enter search criteria.
  <% end %>
</div>
