<!DOCTYPE html>
<html>
<head>
  <%= render partial: 'shared/logged_in_header' %>
  <style>
    .profile-image {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      overflow: hidden;
    }
    .favorite-title {
      font-size: 2rem; /* Adjust the size as needed */
    }
    .search-title {
      font-size: 2rem; /* Adjust the size as needed */
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <div class="row">
      <div class="col-md-7 favorite-videos">
        <h3 class="favorite-title">Favorite Videos</h3>
        <ul id="favorite-videos-list">
          <%= render partial: 'videos/favorite_videos', locals: { favorite_videos: @favorite_videos } %>
        </ul>
      </div>
      
      <div class="col-md-5">
        <h3 class="search-title">
          Search for videos!
          <% if @videos.present? %>
           <%= button_to 'Clear Search Results', user_path(@user.id, clear_search: true), method: :get, remote: true, class: 'btn btn-danger' %>
          <% end %>
        </h3>
        <div class="search-form">
          <%= render partial: 'search_form' %>
        </div>

        <div id="search-results" class="search-results mt-4">
          <%= render partial: 'search/results', locals: { videos: @videos } %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
